# WS9 Stage 4 (4.3–4.4) — live acceptance PASSED; Stage 4 complete

**Session id:** `6c2c46f8-4198-41bf-ba18-5495246048e4`
**Date:** 2026-0711-0237 · **Repo:** this repo only (branch `roh/phase-b`); herdr (Rust)
untouched.
**Continues:** `2026-0711-0224-ws9-stage-4.1-4.2-gateway2.md` (same code, different session).

> **Verification session.** Ran WS9 Stage 4 tasks **4.3 (live acceptance) and 4.4
> (import-graph check)** from `ai_docs/phase-c-ws9-tasks.md` against a live termhost
> daemon + gateway2 + headless Chrome. **All checks passed; Stage 4 (and with it the
> WS9 proof harness) is done.** Only code change: `click_text` op added to wsprobe2.

---

## Code change

- `cmd/wsprobe2/main.go`: new op **`click_text:PANE:TEXT`** — polls (expect-style) until
  TEXT appears in the pane grid, then mouse down+up on its first cell; backed by
  `paneGrid.find` (cell-per-rune match so coords stay column-accurate). This was the
  "planned but not added" op from the previous session; it made the htop click
  deterministic. Doc comment updated.
- `ai_docs/phase-c-ws9-tasks.md`: 4.3/4.4 checked off.

## Test rig (all torn down at session end)

- `termhost -socket /tmp/herdr-gw2test.sock -persistent` (test socket, NOT the default —
  avoids any live daemon), `gateway2 -addr :8431 -socket /tmp/herdr-gw2test.sock`,
  binaries in the session scratchpad. termhost + gateway2 built `-tags ghostty` with
  `PKG_CONFIG_PATH=~/projs/rust/herdr/vendor/libghostty-vt/zig-out/share/pkgconfig`;
  wsprobe2 untagged.

## 4.3 acceptance — what passed and how

1. **Two live shells + focus routing**: `dump:0` showed both 60x31 inner grids and layout
   (rect 60x32 each, inner minus 1 chrome row). Typed `ZETA2` → landed in initially-focused
   pane 2; `focus:1` + `ALPHA1` → pane 1, `absent:2:ALPHA1` ✓; layout rebroadcast flipped
   `focused` flags.
2. **htop mouse**: `modes:1:mouse` confirmed capture mirrored via `pane_modes`. Footer
   "F10Quit" is cut off at 60 cols, so the deterministic click target became the **MEM%
   column header**: `absent:1:MEM%▽; click_text:1:MEM%` → `expect:1:MEM%▽` ✓ (click at
   cell 45,9 flipped htop's sort — proves inner-rect 0-based coords + encoder `+1` + SGR
   path end-to-end). Wheel accepted in alt-screen. `key:F10` quit htop → `modes:1:nomouse` ✓.
3. **Kitty keys**: helper typed into the pane:
   `printf '\033[>1u' && stty -icanon -echo min 5 time 20 && dd bs=1 count=5 | cat -v && stty sane && printf '\033[<u' && echo KDONE`
   (`&&` not `;` — semicolons would split wsprobe2's op script). Then `key:Escape` →
   grid showed **`^[[27uKDONE`**: Escape encoded as CSI-u, i.e. the pane's kitty flags
   reached the server-side encoder via the β `pane_modes` mirror. Wrong path would show
   bare `^[` (dd times out at 2s with 1 byte).
4. **pane_agent**: `internal/detect` manifests: claude/codex/pi/copilot/agy/gemini. Fake
   `codex` shell script on PATH, run in pane 2 → `← pane_agent pane=2 agent="codex"
   state=idle` + Agents rollups; after Ctrl+C agent cleared to `""/unknown`, and a fresh
   connection replays the cached (cleared) state.
5. **Restart survival**: (a) marker `SURVIVE-GW` in pane 2 → kill gateway2 → restart →
   new probe sees the marker (deterministic pane ids 1,2 adopted from `welcome.panes` +
   resync). (b) kill+restart daemon → gateway auto-redialed in ~3s, reconcile recreated
   both panes (fresh prompts, marker gone), input routing still worked (`POSTRESTART`).
6. **Visual (headless Chrome screenshot of :8431)**: two canvases side by side, colored
   prompt (packed-u32 resolve), chrome strips with `w1:p1`/`w1:p2` (focused strip
   highlighted), blue focus outline + cursor only on focused pane, header
   `herdr gateway2 · ● herdr-web · tabs: [1]`, status `connected · 159×46`; the browser's
   own resize propagated to the daemon panes. Chrome CLI:
   `--headless=new --timeout=8000 --screenshot=... http://localhost:8431`.

## 4.4 — import graph

- `go list -deps -tags ghostty ./cmd/gateway2 ./cmd/wsprobe2 | grep -E 'internal/wire|internal/herdrconn'`
  → **empty**. Old `cmd/gateway` (γ path) still builds `-tags ghostty`. Untagged
  `go build ./...` + `go test ./...` and tagged `go test -tags ghostty ./...` all green.

## Notes / leftovers

- Probe `expect` can match the *typed* command echo, not just output — routing proofs
  used `absent` on the other pane to disambiguate.
- Pane cwd/title stayed `""` — this zsh setup doesn't emit OSC 7/0; not a harness bug.
- Pre-existing lint nit in wsprobe2 (`for i := 0; i < n; i++` modernizable, line ~651) —
  untouched, matches file style.
- Still flagged from earlier sessions, not done: live_handoff harness fix (leaked attached
  `herdr server` clients defeat the daemon idle reaper).
- **WS9 Stage 4 complete** → next is whatever the phase plan sequences after WS9
  (see `ai_docs/phase-c-ws9-tasks.md` out-of-scope list: WS10 auth/TLS, WS8 styled chrome,
  WS3 tagged-union Node, WS11 wire/herdrconn removal).
